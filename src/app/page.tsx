import { cn } from "@/lib/utils";

export default async function Home() {
  return (
    <main className={cn("bg-red-500")}>
      <p className={cn("")}>
        welcome to ajiz website
      </p>
    </main>
  );
}
